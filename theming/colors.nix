# Static fallback colors for Hyprland border defaults at startup, before
# GavBar's theme-switch.sh runs (Scripts/bash/theme-hooks/hyprland.sh
# overrides these with wallpaper-derived values via hyprctl keyword).
{
  overlay = "#524535";
  subtle  = "#908caa";
  accent  = "#e7d6a7";
}
