# Static fallback colors for Hyprland border defaults at startup, before
# theme-switch.sh runs and theming/hooks/hyprland.sh overrides them with
# wallpaper-derived values via hyprctl keyword.
{
  overlay = "#524535";
  subtle  = "#908caa";
  accent  = "#e7d6a7";
}
